# BUGLOG.md — Nhật ký lỗi GiuPay

> Mỗi lỗi 1 dòng: **triệu chứng → nguyên nhân → cách sửa**. Ghi ngay khi sửa xong.
> Trước khi debug lỗi mới, **đọc mục "Lỗi hay gặp" bên dưới trước** — phần lớn là môi trường, không phải code.

---

## ⚡ Lỗi HAY GẶP (kiểm tra mấy cái này TRƯỚC khi đụng code)

| Triệu chứng | Nguyên nhân thường gặp | Cách xử lý |
|---|---|---|
| Sửa code xong **không thấy thay đổi** | Đang chạy `npm start` (bản build cũ), không phải `npm run dev` | `npm run build` lại, hoặc chuyển sang `npm run dev`. Backend chạy `node dist/` cũng vậy. |
| `429 Too Many Requests` (rpc.testnet.arc.network) | RPC công cộng bị rate-limit, thường do **mở nhiều tab** cùng lúc | Đóng bớt tab, chờ ~30s. Lâu dài: dùng RPC riêng / thêm RPC dự phòng, giãn `pollingInterval`. |
| **Không thanh toán được**, ví kẹt "Đang ước tính", nút Xác nhận mờ | 2 nguyên nhân khác nhau, CÙNG triệu chứng: (1) Ví không tính được gas vì RPC bị 429/-32011 (2) `pay()` sẽ **revert** khi ví simulate — thường vì `shop` chưa `isVerified()` on-chain | (1) Đóng bớt tab / kiểm tra có 2 bộ `dev:all` chạy trùng không (xem PowerShell `Get-CimInstance Win32_Process -Filter "name='node.exe'"`). (2) Xem mục "Shop seed chưa verify on-chain" bên dưới. Muốn biết chắc lý do nào: `eth_call` mô phỏng thẳng `pay()` bằng ethers, đọc `e.reason` — chain trả revert reason thật, khỏi đoán. |
| Trả tiền Arc cho **shop demo/seed** (demo-1..28, "Shop Test"...) luôn revert `"Shop not verified"` dù DB ghi `shop_verified: true` | `seed.ts` tạo shop bằng `INSERT ... status='verified'` thẳng SQL — bỏ qua bước gọi `verifyShopOnChain()` mà luồng admin thật vẫn làm (`admin.ts` duyệt shop → tự verify on-chain). Shop seed chỉ "verified" trên DB, KHÔNG verified trên `ShopRegistry` contract | Shop tạo qua luồng thật (Đăng ký shop → Admin duyệt ở `/admin`) tự verify on-chain, không cần làm gì thêm — chỉ shop seed mới dính. Muốn test payment Arc: xóa shop seed, tạo shop mới qua form + admin duyệt thật. |
| Ví báo `chain mismatch` dù đã đổi mạng thủ công | State `useChainId()` không đồng bộ mạng thật của ví (OKX không bắn event) | Luôn gọi `switchChainAsync()` TRƯỚC khi ký (xem `lib/ensure-chain-write.ts`). |
| Đơn `in_escrow` nhưng **SBT không mint** (My SBTs = 0) | Thiếu `DEPLOYER_PRIVATE_KEY`/`SBT_CONTRACT_ADDRESS`, ví deployer hết gas, RPC 429, hoặc địa chỉ SBT frontend≠backend | Xem log terminal backend sau khi trả tiền; đối chiếu `.env`; đảm bảo `NEXT_PUBLIC_SBT_CONTRACT` == `SBT_CONTRACT_ADDRESS`. |
| Admin thao tác báo lỗi sau khi restart backend | Session admin lưu in-memory, restart là mất | Đăng nhập lại `/admin` (email+TOTP). |
| Đổi `.env` không ăn | Chưa restart process đọc env | Restart backend/frontend. |
| Ví (OKX/MetaMask) kẹt "Đang ước tính" / "Giao dịch chưa xác định" khi ký `pay()`, dù RPC server-side đã xác nhận khỏe (không -32011, `eth_call` simulate không revert) | Ví lưu **RPC riêng đã add từ trước** cho mạng Arc Testnet (thường là `rpc.testnet.arc.network` — RPC công khai cũ). App gọi `wallet_addEthereumChain`/`switchChain` với RPC mới (Alchemy) nhưng **ví KHÔNG tự ghi đè** RPC đã lưu cho mạng đã tồn tại sẵn — vẫn tự hỏi RPC công khai cũ (hay nghẽn vì dùng chung toàn thế giới), độc lập hoàn toàn với indexer/relayer/frontend của mình | Vào ví → Quản lý mạng (Manage Networks) → Arc Testnet → sửa tay ô "URL RPC" thành đúng URL Alchemy đang dùng trong `.env` (`NEXT_PUBLIC_ARC_RPC_URL`). **OKX Wallet KHÔNG cho sửa RPC của custom network đã add** (ô RPC bị khóa/không thao tác được) — phải chuyển sang ví khác (đã thử MetaMask: Add network thủ công với đúng RPC Alchemy → ký được ngay). Không có cách nào ép ví qua code — EIP-3085 không bắt buộc ví ghi đè RPC cho mạng đã add. |
| **QUYẾT ĐỊNH SẢN PHẨM (2026-07-19): OKX Wallet đã bị ẩn khỏi danh sách chọn ví.** Không phải bug — chủ đích. | Bug RPC cache của OKX (dòng trên) không sửa được từ phía code — nếu 1 user đã add mạng Arc bằng OKX trước khi app đổi RPC, họ sẽ **kẹt vĩnh viễn** không cách nào tự sửa (OKX không cho edit RPC network đã add). Không muốn user mới dính lỗi này. | `wagmi.config.ts`: đổi từ `getDefaultConfig()` sang `connectorsForWallets([...])` (chỉ liệt kê MetaMask/WalletConnect/Coinbase/Rainbow) + `multiInjectedProviderDiscovery:false` (chặn hẳn cơ chế tự phát hiện ví ngoài danh sách, kể cả ví tự "vẫy tay" qua EIP-6963 như OKX). **Side-effect phải vá:** `e2e/helpers/mock-wallet.ts` (ví giả dùng cho test) cũng bị ẩn theo vì dùng chung cơ chế EIP-6963 announce — sửa bằng cách set `isRainbow=true` trên provider giả (RainbowKit's `rainbowWallet` connector check qua `window.ethereum.isRainbow` trực tiếp, không qua EIP-6963/rdns — xem comment trong file). |

---

## 📒 Nhật ký lỗi đã sửa

| Ngày | Triệu chứng | Nguyên nhân | Cách sửa |
|---|---|---|---|
| 2026-08-11 | Toàn app crash "Application error: a client-side exception has occurred" ở MỌI trang (không riêng lúc bấm connect) | Console: `Error: invalid border=0` từ `_app` chunk. RainbowKit dùng `cuer` (v0.0.3) để vẽ QR WalletConnect; `cuer` khai `"qr": "~0"` (mọi bản 0.x) nên npm kéo `qr@0.6.0` - bản mới bắt buộc `border>0`, từ chối `border=0`. `cuer` vẫn gọi `qr` với `border:0` như code cũ → throw. Vì RainbowKit provider mount toàn cục trong `_app.tsx`, lỗi này sập cả app chứ không riêng modal connect | Ghim `qr` về `0.5.5` (bản trước khi đổi rule, chỉ kiểm `typeof border` chứ không chặn `<=0`) qua `overrides` trong `package.json`. Không đụng code app - lỗi ở thư viện ngoài (`cuer`) chưa theo kịp `qr`. |
| 2026-08-11 | Test E2E ví thật (dAppwright/MetaMask) flow pay: chữ ký thứ 2 (pay) treo tới hết timeout, `browserContext.waitForEvent: Target page... closed` | `confirmTransaction` của dAppwright dùng `context.waitForEvent("page")` để bắt popup MetaMask **mới**. Popup pay tự bật sau khi approve mined (không do click). Nếu chèn `waitForTimeout` giữa 2 lần `confirmTransaction`, popup pay mở trong lúc chờ → listener của lần 2 arm SAU khi event đã xảy ra → chờ mãi | Gọi 2 `confirmTransaction()` **liền nhau, không chờ giữa**. Lần 2 arm `waitForEvent` ngay, bắt được popup pay khi approve mined xong. (Test timeout để rộng ~360s cho approve mine trên Arc.) |
| 2026-08-11 | Synpress v4 build cache fail trên Windows: "Sorry, Windows is currently not supported. Please use WSL instead!" | Synpress v4 CLI build cache chỉ chạy Linux/macOS/WSL | Đổi sang **dAppwright** (`@tenkeylabs/dappwright`) - cũng drive MetaMask thật, thuần Playwright, chạy Windows OK. Cần `npx playwright install chromium` (dappwright bump playwright-core lên bản cần chromium-1140). |
| 2026-08-11 | Test pay: nút "Kết nối ví để thanh toán" luôn disabled / dropdown huyện-xã GHN không load | Form ship bắt buộc chọn tỉnh/huyện/xã khi GHN có data. Data GHN test lẫn nhiều entry rác (tỉnh "Hà Nội 02", huyện "Quận Đặc Biệt" 0 con) - chọn theo index 1 trúng entry rỗng nên huyện/xã không có → shipValid=false | Resolve động qua `/api/ghn-master`: quét tỉnh→huyện→xã tìm chuỗi CÓ data thật, rồi `selectOption` theo value. KHÔNG phải bug prod (tỉnh thật vẫn có huyện). |
| 2026-08-11 | Test pay: trang /pay báo "Chưa có đơn hàng nào" dù đã tạo đơn qua API | Tạo đơn trên backend onrender nhưng frontend LOCAL đọc order từ `NEXT_PUBLIC_API_URL=localhost:3001` (backend local không chạy) → lệch nguồn | Cho test chạy thẳng vào app deployed (baseURL Vercel) đã nối onrender, khớp nguồn + tránh CORS. |
| 2026-07-19 | Thanh toán Arc trực tiếp: sau khi ký `approve()` xong, ví (OKX) hiện popup `pay()` nhưng nút Xác nhận bị **mờ**, không ký được. Ví báo `-32011 "request limit reached"` ngay khi ước tính gas | KHÔNG phải bug code `pay()` (đã kiểm bằng burst-test RPC: 40 request đồng thời tới Alchemy vẫn 0 lỗi khi chỉ chạy 1 bộ). Nguyên nhân là **2 bộ `dev:all` chạy song song** (quên tắt bộ cũ trước khi bật lại) → 2 indexer + 2 cctp-relayer poll trùng, dồn request vào 1 key Alchemy free-tier → tràn đúng lúc ví ước tính gas cho `pay()`. Tái diễn của lỗi 2026-07-18 (9 node.exe), lần này chỉ 2 bộ nhưng cùng gốc | Tắt sạch tiến trình `dev:all` cũ qua Task Manager/PowerShell (`Stop-Process` theo cmdline `indexer\|cctp-relayer\|server.ts\|next dev\|concurrently`), khởi động lại đúng **1 bộ**. **Nhắc: luôn kiểm `node.exe` trước khi chạy `npm run dev:all`, đừng chạy chồng lệnh cũ.** |
| 2026-07-19 | Ví có shop **verified** connect lần đầu (qua modal RainbowKit) — NavBar nhận đúng shop (hiện nút "Shop của tôi") nhưng **không tự chuyển** sang `/shop/{id}` như ý code | `NavBar.tsx` dùng cờ `hasMounted.current` để tính `freshConnect` (chỉ auto-redirect khi vừa connect lần đầu, không redirect khi đổi ví). Cờ này chỉ được set `true` ở nhánh **đã có** địa chỉ ví — nhánh **chưa có** ví (`if (!address) {...return}`) không set, nên `freshConnect` không bao giờ true đúng lúc cần → `router.push` chết. Phát hiện qua viết lại `wallet-flows.spec.ts` (test cũ trỏ `/select-account` đã xóa, viết lại theo luồng connect mới thì lộ ra bug này) | Thêm `hasMounted.current = true` vào nhánh chưa có ví (`NavBar.tsx` ~dòng 139). Đã kiểm 3 tình huống qua test: connect verified lần đầu → redirect đúng; đã connect sẵn rồi reload → không bị giật; đổi ví → không tự nhảy trang |
| 2026-07-19 | `next build` fail: `NavBar.tsx:380` — "Cannot invoke an object which is possibly 'undefined'" | `openConnectModal` lấy từ RainbowKit `useConnectModal()` có kiểu `(()=>void)\|undefined`; dòng 380 (nút Connect ví ở menu mobile) gọi trực tiếp `openConnectModal()`. Dòng 298 không lỗi vì chỉ truyền tham chiếu cho `onClick` | Thêm optional chaining `openConnectModal?.()` (giống cách dòng 298 dung nạp undefined). Chỉ ảnh hưởng nút Connect trong menu mobile, không đụng chỗ khác |
| 2026-07-18 | Ví báo "sai contract" lúc approve() trên Arc, kèm 429 dồn dập ở console. **Vẫn còn tái diễn sau lần sửa đầu** → sửa tiếp lần 2, triệt để hơn | Không phải sai contract (địa chỉ USDC ERC-20 interface `0x3600...0000` là đúng, chính thức của Circle) — mà là RPC Arc testnet bị rate-limit 429. 3 nguồn cộng dồn: (1) `ConnectButton.Custom` ở NavBar/NavBarMinimal âm thầm gọi `useBalance()` trên MỌI trang dù không hiển thị số dư ra UI, (2) `indexer.ts` gọi `getBlockNumber()` + nhiều `queryFilter()` MỖI 15s vô điều kiện dù không có gì mới, (3) `cctp-relayer.ts` mỗi 20s (nhẹ hơn, chỉ gọi RPC khi có đơn đang bridge) | Lần 1: thay `ConnectButton.Custom` bằng `useConnectModal`/`useAccountModal` ở `NavBar.tsx` + `NavBarMinimal.tsx`. Lần 2 (sau khi 429 vẫn còn): giãn `POLL_MS` mặc định `indexer.ts` 15s→30s, `cctp-relayer.ts` 20s→30s; đổi cả 2 từ `setInterval` cố định sang `setTimeout` đệ quy có backoff — gặp 429 thì tự gấp đôi khoảng chờ (tối đa 2 phút, chỉnh qua `INDEXER_MAX_BACKOFF_MS`/`CCTP_RELAYER_MAX_BACKOFF_MS`), thành công thì về lại nhịp bình thường. |
| 2026-07-18 | `useWallet.ts` hardcode `ARC_TESTNET_CHAIN_ID = 1515`, lệch với chain ID thật `5042002` dùng ở `wagmi.config.ts` | Giá trị cũ chưa từng khớp, có lẽ ghi nhầm lúc mới viết | Đổi sang đọc cùng biến env `NEXT_PUBLIC_ARC_CHAIN_ID` (fallback 5042002) như `wagmi.config.ts` — đã kiểm tra `isOnArc`/`switchToArc` chưa có nơi nào khác gọi nên không ảnh hưởng hành vi hiện tại. |
| 2026-07-18 | Đổi RPC Arc sang Alchemy riêng xong vẫn báo lỗi `-32011 request limit reached` (eth_chainId) ngay lập tức | Task Manager có 9 process `node.exe` — các lần restart trước chưa tắt hết bản cũ, nhiều bản `indexer`/`cctp-relayer` cùng chạy song song, dồn request vào 1 API key | Hướng dẫn user tắt hết `node.exe` qua Task Manager rồi mở lại đúng 1 lần mỗi tiến trình. Thêm script `dev:all` ở root `package.json` (dùng `concurrently`) để gộp backend+indexer+cctp-relayer+frontend thành 1 lệnh, tránh lặp lại tình trạng quên tắt process cũ. |
| 2026-07-18 | `eth_getLogs` báo lỗi `-32600`: "Under the Free tier plan, you can make eth_getLogs requests with up to a 10 block range" | `indexer.ts` quét theo lô `BATCH_BLOCKS=2000` block/lần — vượt xa giới hạn cứng 10 block/lệnh của gói Alchemy free tier. Phát hiện thêm khi soát log: `cctp-relayer.ts` import `orderIdFromCode` từ `./indexer` — indexer.ts có side-effect ở cuối file (tự gọi `runLoop()` khi bị import, không chỉ khi chạy trực tiếp) → mỗi lần cctp-relayer khởi động, nó CHẠY LUÔN 1 bản indexer thứ 2 bên trong, nhân đôi tải RPC | Giảm `BATCH_BLOCKS` mặc định 2000→10 (bắt buộc theo giới hạn nhà cung cấp, chỉnh qua `INDEXER_BATCH_BLOCKS` nếu đổi tier). Sửa `cctp-relayer.ts` import `orderIdFromCode` từ `escrow-chain.ts` (cùng logic, không side-effect) thay vì từ `indexer.ts` — chấm dứt việc chạy trùng 2 vòng lặp indexer. **Đánh đổi:** batch nhỏ hơn nghĩa là quét cùng khoảng block cần nhiều lệnh `getLogs` hơn — nếu vẫn còn bị rate-limit theo request/giây, bước tiếp theo nên gộp 3-4 event filter hiện đang gọi riêng lẻ mỗi batch thành 1 lệnh `getLogs` duy nhất (dùng mảng topic OR). |
| 2026-07-16 | Tin nhắn buyer gửi shop **không tới** (khi chat trước khi mua) | Danh sách hội thoại 2 phía dựng từ bảng `orders`, không phải `messages` — người chưa mua không hiện | `UNION` orders + messages trong `GET /conversations` & `/shop-conversations` (`messages.ts`) |
| 2026-07-16 | Đang ở `/shop/{id}`, đổi ví → **văng `/register`** | `DashboardPage` hard-redirect `/register` khi `/me` trả 404 | Bỏ redirect; để wrapper `/shop/[id]` tự đổi sang buyer view |
| 2026-07-16 | Ảnh shop không hiện ở trang "My Shop" (Dashboard) | Dashboard header không render ảnh, dù data có `logo_cid` | Thêm component `ShopAvatar` (bấm xem full) vào Dashboard + trang khách |
| 2026-07-13 | Ví báo `chain mismatch` dù đã đổi mạng | Check `currentChainId` state không khớp ví thật | `lib/ensure-chain-write.ts` luôn switch chain trước khi ký |
| 2026-07-13 | Icon dấu tích lệch trái dù `textAlign:center` | Phosphor render `<svg display:block>`, text-align không center được | Thêm `display:block; margin:0 auto` vào icon |
| 2026-07-13 | **Không đánh giá được** (review luôn trống) | Thiếu route `POST /:code/review` + không có link tới `/review` | Thêm route backend + nút "Đánh giá" ở Profile/order page |
| 2026-07-13 | **SBT không bao giờ mint** | `mint()` chưa từng được gọi ở đâu trong backend | Thêm `sbt-chain.ts`, gọi `mintWarrantySBT()` lúc đơn vào `in_escrow` |
| 2026-07-14 | Ảnh sản phẩm ở `/products` luôn là ảnh demo | `fetchRealProducts()` thiếu map `image_cid` | Thêm `imageCid` vào interface + map |
| 2026-07-14 | Shop không hiện logo/ảnh thật | Route `/:id/full` tự dựng response, rớt `logo_cid`/`wallet_address` | Thêm `logoCid`, `walletAddress` vào response |
| 2026-07-14 | Admin resolve tranh chấp báo `401 Unauthorized` | Route còn dùng `requireAuth0` (Auth0) trong khi admin đã đổi sang `X-Admin-Session` | Đổi middleware sang `requireAdminSession` |
| 2026-07-12 | Admin "Xác nhận quyết định" luôn **404 âm thầm** | Dùng `dispute.id` thay cho `order_code`, không check `res.ok` | Dùng đúng `order_code` + kiểm tra kết quả thật |
| 2026-07-12 | Mọi lỗi API đều trả "Lỗi server" chung chung | Logic `NODE_ENV==="development"` sai chiều → luôn ẩn message thật | Trả `err.message` thật (nhớ đổi lại trước production) |
| 2026-07-11 | Admin/shop resolve tranh chấp **không gọi contract**, tiền kẹt escrow | Dòng gọi contract bị comment `// TODO` | Nối `escrow-chain.ts`, gọi `adminResolve()`/`refundByShop()` thật trước khi ghi DB |
| 2026-07-11 | Dashboard shop không refresh sau khi giao hàng | `DashboardPage` không truyền prop `onShipped` cho modal | Thêm `loadOrders()` tách riêng, truyền vào `onShipped` |

---

## ⚠️ Giới hạn đã biết (không phải bug — đừng sửa nhầm)

- Đơn trả **trực tiếp trên Arc** (không qua CCTP): chỉ **buyer** mới `openDispute()` được (contract bắt `msg.sender==buyer`), admin/bot không mở hộ được. Buyer phải tự mở tranh chấp on-chain trước khi admin resolve.
- Review hiện **chỉ lưu off-chain** (Postgres), dù vài chỗ chữ nói "ghi lên Arc". Cố ý, theo yêu cầu.
- Mã vận đơn GHN hiện là **giả** (`"GHN"+random`), chưa gọi API GHN thật.
